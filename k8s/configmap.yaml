apiVersion: v1
kind: ConfigMap
metadata:
  name: aks-demo-config
  namespace: demo-app
  labels:
    app: aks-demo
data:
  APP_VERSION: "2.0.0"
  ENVIRONMENT: "production"
  NODE_ENV: "production"
  LOG_LEVEL: "INFO"
